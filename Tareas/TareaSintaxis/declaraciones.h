#include <iostream>
using namespace std;

// procedimiento dummy de prueba del profesor
void dummy(int);
//un procedimiento que retorne el menos de los digitos decimales de un n�mero entero
// ejemplo: el menor de 95246287 es 2
int getDigitoMenor(int);
// un procedimiento que retone el mayor valor de un arreglo de enteros
int getMayor (int *, int);
// un procedimiento que invierta el orden de los valores de un arreglo de enteros
void invertirArreglo (int *, int);
// un procedimiento que retorne un nuevo arreglo concatenando dos arreglos dados
// ejemplo: {1,2,3,4} con {10,20} retornar� un nuevo arreglo con {1,2,3,4,10,20}
void concatenarArreglos (int *, int, int *, int);
